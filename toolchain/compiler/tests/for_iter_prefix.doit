# AI-generated test
# Prefix match: bind fewer variables than iterator yields
behavior for_iter_prefix {
    @name "For Iter Prefix"

    for comp in each_component() {
        notify "looping"
    }

    notify "done"
}
