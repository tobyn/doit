fn is_big(a) exec(yes, no) {
    if a > 5 {
        return yes
    }
    return no
}

behavior "Exec Expr" {
    var x = 10
    let result = is_big(x) {
        yes { 1 }
        no { 0 }
    }
    notify "done", value: result
}
