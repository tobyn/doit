# AI-generated test
# Behavior-level if with || boolean chain condition
behavior bhv_if_or {
    @param in a "A"
    @param in b "B"
    if $a > 10 || $b > 10 {
        notify "at least one is big"
    }
}
