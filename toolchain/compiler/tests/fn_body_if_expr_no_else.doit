# AI-generated test
# If expression without else in fn body
behavior fn_body_if_expr_no_else {
    @name "Fn Body If Expr No Else"
    @param in a "A"
    let x = f $a
    notify "done", value: x
}

fn f(v) {
    let x = if v > 1 { 5 }
    return x
}
