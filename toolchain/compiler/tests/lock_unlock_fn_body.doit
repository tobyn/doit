# AI-generated test
# lock/unlock inside user-defined function, inlined correctly
fn fast_notify(txt) {
    unlock
    notify txt
    lock
}

behavior lock_unlock_fn_body {
    fast_notify "Speedy"
}
