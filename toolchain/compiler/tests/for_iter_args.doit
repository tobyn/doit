# AI-generated test
# Iterator with positional and keyword args
behavior for_iter_args {
    @name "For Iter Args"

    for i in for_number(0, 10, step: 2) {
        notify "looping"
    }

    notify "done"
}
