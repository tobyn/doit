# AI-generated test
# Function call in non-first boolean position
fn get_flag() {
    return instruction "get_var" {
        0: "flag"
        1: @1
    }
}

behavior bool_fn_call {
    @name "Bool Fn Call"
    @param in d "d"
    let r = $d || get_flag
}
