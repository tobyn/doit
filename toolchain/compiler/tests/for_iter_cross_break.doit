# AI-generated test
# Cross-iterator labeled break: from inner Range to outer iterator
behavior for_iter_cross_break {
    @name "For Iter Cross Break"

    'outer: for comp, idx in each_component() {
        for item in each_inventory_item() {
            if item == null {
                break 'outer
            }
        }
    }

    notify "done"
}
