# AI-generated test
# Redundant lock at start (already locked) and redundant unlock after unlock
# are eliminated by the compiler
behavior lock_unlock_optimize {
    lock
    notify "Already locked"
    unlock
    unlock
    notify "Still unlocked"
    lock
    lock
    notify "Still locked"
}
