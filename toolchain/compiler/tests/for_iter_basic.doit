# AI-generated test
# Basic instruction-backed iterator: for comp, idx in for_component()
behavior for_iter_basic {
    @name "For Iter Basic"

    for comp, idx in for_component() {
        notify "looping"
    }

    notify "done"
}
