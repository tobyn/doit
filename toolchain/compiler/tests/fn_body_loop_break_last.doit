# AI-generated test
# if/break as last statement in loop body (fn body)
fn counter() {
    var i = 0
    loop {
        i++
        if i >= 3 { break }
    }
    return i
}

behavior fn_body_loop_break_last {
    @name "Fn Loop Break Last"

    let result = counter
    notify "Done"
}
