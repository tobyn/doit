# AI-generated test
# Labeled break targeting outer iterator
behavior for_iter_labeled {
    @name "For Iter Labeled"

    'outer: for comp, idx in for_component() {
        for i in Range(3) {
            if i == 1 {
                break 'outer
            }
        }
    }

    notify "done"
}
