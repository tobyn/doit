---
paths:
  - "**/*.doit"
---

# doit Code Style

## Formatting

All doit source files must be `doit fmt`-clean. Run
`doit fmt -l <files>` to check. If any files are listed, run
`doit fmt -w` on them before considering the change done.
