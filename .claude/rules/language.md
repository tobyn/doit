# The doit language

The doit language is documented in `manual/`. `manual/index.md` is the entrypoint for the documentation.

See `manual/language.md` for language syntax and `manual/functions.md` for functions and the standard library.
